pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                echo 'Done.'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                echo 'Done.'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                script {
                    sh "chmod +x -R ${env.WORKSPACE}"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/api-start.sh"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/chat-start.sh"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/etc-start.sh"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/main-start.sh"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/parser-start.sh"
                    sh "${env.WORKSPACE}/cicd-test/scripts/codedeploy/live/rooms-start.sh"
                }
                echo 'Done.'
            }
        }
    }
}